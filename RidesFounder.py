import log
logger = log.setup_custom_logger('RidesFounder')


def find_a_ride(ride_request):
    logger.info('Trying to find a ride')
    # get all drivers with same start point routes
    # find the top3 routes that nearest to passenger home

    return 'ride founded'


def get_all_drivers_with_same_start_point_routes(passenger_start_point):
    return None


def find_the_top3_options(passenger_end_point):
    return None
